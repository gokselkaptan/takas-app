{
  "buildCommand": "cd /vercel/path0 && printf '' > yarn.lock && prisma generate && next build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/keep-alive",
      "schedule": "*/4 * * * *"
    }
  ]
}
