{
  "installCommand": "npm install && cp /dev/null yarn.lock",
  "buildCommand": "prisma generate && next build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/keep-alive",
      "schedule": "*/4 * * * *"
    }
  ]
}
